const express=require('express'),WebSocket=require('ws'),http=require('http'),app=express(),server=http.createServer(app),wss=new WebSocket.Server({server,path:'/ws'});app.use(express.static('public'));wss.on('connection',ws=>{console.log('🔗 Volt');ws.on('message',d=>{try{const m=JSON.parse(d)||d;console.log('BTN:',m.button||m);wss.clients.forEach(c=>{if(c.readyState===WebSocket.OPEN)c.send(typeof m==='string'?m:JSON.stringify(m))})}catch(e){wss.clients.forEach(c=>c.readyState===WebSocket.OPEN&&c.send(d))}})});server.listen(process.env.PORT||8080)
